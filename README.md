# Wineo Admin

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 19.2.21.

## Development server

Run `ng serve` and open `http://localhost:4200/`.

## Build

Run `ng build`. Output is in `dist/wineo-admin`.
